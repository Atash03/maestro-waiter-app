/**
 * Custom Hooks Index
 *
 * Central export point for all custom hooks
 */

export {
  type ProtectedRouteState,
  useAuthCallbacks,
  useIsRouteProtected,
  useProtectedRoute,
} from './useProtectedRoute';
